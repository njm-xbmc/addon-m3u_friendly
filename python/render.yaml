services:
  - type: web
    name: addon-m3u-iptv
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn addon:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 7000
